mutation createApplication($input: CreateApplicationInput!){
    createApplication(input: $input){
        id
        userId
        position{
            id
            positionName
            role
            company
            startsAt
            endsAt
        }
    }
}
