mutation createPosition($input: CreatePositionInput!){
    createPosition(input: $input){
        id
        userId
        positionName
        company
        role
        startsAt
        endsAt
    }
}
