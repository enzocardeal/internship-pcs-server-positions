query getPositionById($input: GetPositionByIdInput!) {
    getPositionById(input: $input) {
        id
        userId
        company
        positionName
        role
        description
        startsAt
        endsAt
        steps
        benefits
        compensation
        area
        location
        scholarship
    }
}
