query getPositionById($input: GetPositionByIdInput!) {
    getPositionById(input: $input) {
        id
        userId
        company
        positionName
        role
        startsAt
        endsAt
        steps
        benefits
        area
        location
        scholarship
    }
}
