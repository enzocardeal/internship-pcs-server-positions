mutation updatePosition($input: UpdatePositionInput!){
    updatePosition(input: $input){
        id
        userId
        positionName
        company
        role
        startsAt
        endsAt
    }
}
